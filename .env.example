# Fork 子路径部署（默认 /sublink，与 NPM location /sublink/ 对齐）
BASE_PATH=/sublink

# 规则选择区展示的内网入口（可选；仅 scheme+host+端口+路径，勿末尾 /）
LAN_SERVICE_BASE=http://192.168.0.6:58087/sublink

PORT=8787
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_KEY_PREFIX=sublink
CONFIG_TTL_SECONDS=2592000
